const channels = require('./channels.png')
const addingContent = require('./addingContent.png')
const closed = require('./closed.png')
const connect = require('./connect.png')
const editAndComment = require('./editAndComment.png')
const navigation = require('./navigation.png')
const open = require('./open.png')
const _private = require('./private.png')

export default {
  channels,
  addingContent,
  closed,
  connect,
  editAndComment,
  navigation,
  open,
  private: _private,
}

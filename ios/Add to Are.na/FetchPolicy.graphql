
query FetchPolicy {
    me {
        policy {
            AWSAccessKeyId
            acl
            bucket
            expires
            key
            policy
            signature
            success_action_status
        }
    }
}
